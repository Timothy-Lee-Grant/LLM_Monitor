flask
langchain-core
#swagger-ui-py
langchain-community
# Hosted providers (plan 003 Step 4): AzureChatOpenAI / AzureOpenAIEmbeddings /
# ChatOpenAI(base_url=...). Pinned like the MCP adapters — the exact param and
# env-var surface (azure_deployment, api_version, dimensions) was verified
# against this version's wheel before writing the factory branches.
langchain-openai==1.3.5
langchain-ollama
langgraph
langchain-postgres
pgvector
psycopg[binary,pool]
gunicorn

# Observability (plan 002 Step 3) — API is mandatory (no-op spans when disabled);
# SDK/exporter/instrumentation only activate via init_observability().
opentelemetry-sdk
opentelemetry-instrumentation-flask
opentelemetry-exporter-otlp
prometheus-client
langfuse
# langfuse.langchain.CallbackHandler does `import langchain` unconditionally
# (to branch on langchain.__version__ for its v0/v1 shim) even though every
# actual symbol it uses comes from langchain-core — langchain-core alone is
# not enough, this is a hard transitive dependency of the Langfuse integration.
langchain

# ToolBox MCP integration (plan 003 Step 2). Pinned per the walkthrough doc's
# "honest-CI lesson applies to dependencies too" — the adapter API surface
# (transport literals, async-only tools) was verified against exactly this
# version. NOTE: the rest of this file is unpinned (pre-existing debt,
# flagged in plan 003 Stage 4 Step 2 notes).
langchain-mcp-adapters==0.3.0
# mcp is langchain-mcp-adapters' own transitive dependency, pinned separately
# (2026-08-23): an unpinned mcp resolved to 2.0.0 on rebuild, which restructured
# its modules and dropped `RequestContext` from mcp.shared.context, breaking
# langchain-mcp-adapters==0.3.0's import at load time. Verified working against
# this version. See Documentation/AI_Suggestions/002.
mcp==1.29.0

# CPU embeddings for the openai_compat provider (Groq etc. serve none) — see
# app/models/factory.py CPU_EMBEDDING_MODEL. ONNX-based (no torch), installs
# clean on python:3.11-slim with no compiler needed.
fastembed
